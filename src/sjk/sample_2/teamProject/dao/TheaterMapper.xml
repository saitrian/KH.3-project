<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sjk.sample_2.teamProject.dao.TheaterDAO">
	<insert id="inputTheater">
		insert into theater(th_name, th_screen, th_seat, th_address, th_region)
		values(#{theater.th_name}, #{theater.th_screen}, #{theater.th_seat}, #{theater.th_address}, #{theater.th_region})
	</insert>
	<select id="selectThNum" resultType="TheaterVO">
		select * from theater where th_num = #{thNum}
	</select>
</mapper>